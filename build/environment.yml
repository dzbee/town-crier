name: town-crier
channels:
  - defaults
dependencies:
  - flask=1.0.3
  - flask-sqlalchemy=2.4.0
  - flask-wtf=0.14.2
  - pip=19.1.1
  - simplejson=3.16.0 # itsdangerous imports this, but doesn't depend on it
  - pip:
    - flask-bootstrap==3.3.7.1
